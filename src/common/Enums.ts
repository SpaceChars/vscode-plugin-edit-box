/**
 * 窗口弹窗类型
 */
export enum MessageAlertType {
    INFO = 1,
    WARN = 2,
    ERROR = 3
}

/**
 * 页面类型
 */
export enum ViewType {
    TreeView = 0,
    WebView = 1
}

/**
 * 仓库类型
 */
export enum RepositoryType {
    Local = "Local",
    Remote = "Remote"
}
